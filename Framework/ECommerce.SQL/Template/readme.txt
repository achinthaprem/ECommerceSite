ECommerce.SQL LAYER
--------------------------------------------

1.	Code generator should create the SqlData class (a  different one is generated, depending if on ECommerce.SQL Server/Oracle/etc).
	Overwrite the current one there - it is just a stub to allow it to build.
2.	Remove the OracleDataAccess.dll reference if not needed.